export interface IFilters {
    loading? : string,
    filterByTitle : string,
    filterByTag : string,
    sortByDate? :string,
    type? : string,
    filtersVisible? : boolean,
    width?: number, 
    height?: number,
    filterHeight?: number,
    firstLoad? : boolean
}